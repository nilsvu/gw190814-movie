# Overrides to show only the 2-2 mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 2
    Modes: ["(2, 2) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: Greens
