# Overrides to show only the 2-2 Mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 5
    Modes: ["(5, 5) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: Oranges
