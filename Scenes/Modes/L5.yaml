# Overrides to show only the 5-5 mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 5
    Modes: ["(5, 5) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: Oranges
