# Overrides to show only the 2-2 Mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 3
    Modes: ["(3, 3) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: Reds
