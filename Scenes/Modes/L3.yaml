# Overrides to show only the 3-3 mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 3
    Modes: ["(3, 3) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: RdPu
