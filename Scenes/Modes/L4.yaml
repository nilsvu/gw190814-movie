# Overrides to show only the 4-4 mode of the strain

WaveformToVolume:
  Replace:
    EllMax: 4
    Modes: ["(4, 4) Mode"]

TransferFunctions:
  - Element: 0
    TransferFunction:
      Peaks:
        Replace:
          Colormap: PuBu
